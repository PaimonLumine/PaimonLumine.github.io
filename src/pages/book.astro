---
import { getCollection, render } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const posts = await getCollection('posts');
const bookPost = posts.find((p) => p.data.labels.includes('Book'));

let Content = null;
if (bookPost) {
  const rendered = await render(bookPost);
  Content = rendered.Content;
}
---

<BaseLayout title="書單" description="書單">
  <article class="mx-auto max-w-3xl px-4 py-8">
    <h1 class="mb-8 font-[var(--font-title)] text-3xl text-[var(--color-paper)]">書單</h1>
    {Content ? (
      <div class="prose">
        <Content />
      </div>
    ) : (
      <p class="text-[var(--color-paper-dim)]">No content yet.</p>
    )}
  </article>
</BaseLayout>
